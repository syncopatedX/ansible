galaxy_info:
  author: Gregory Furlong
  description: "Install packages from the aur and configure trusted signing keys for sources validation."
  license: MIT
  min_ansible_version: "2.12.0"
  galaxy_tags: [ "system", "aur" ]
  platforms:
  - name: ArchLinux
    versions:
      - all

dependencies:
  - role: gnfzdz.archlinux.aur_setup
