---
- hosts: localhost
  remote_user: root
  roles:
    - window-manager
  vars:
    window_manager: i3
    ansible.builtin.user:
      name: test
      group: test
      home: /home/test
      uid: 1000
