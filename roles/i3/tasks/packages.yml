---

- block:
    - name: Install wm packages
      aur:
        use: paru
        name: "{{ item }}"
        state: present
      with_items:
        - "{{ packages.wm }}"
        - "{{ packages.desktop }}"
  rescue:
    - name: installing packages failed
      debug:
        msg: "Failed to install shell packages"

